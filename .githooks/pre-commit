#!/usr/bin/env bash
# Blocking pre-commit gate: `make precommit` (check + deadcode) in orchestrator/.
# Enabled per clone via `make install-hooks`, which points core.hooksPath here.
set -euo pipefail
cd "$(git rev-parse --show-toplevel)/orchestrator"
exec make precommit
