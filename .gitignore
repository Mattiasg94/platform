# Secrets
.env
.env.*

# Go build output
/bin/
*.exe

# The fixture repo is its own standalone git repo (a stand-in for any customer
# repo the agent edits); the platform does not track it.
demo-project/
